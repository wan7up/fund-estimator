# 基金工具箱协作说明

本项目现在采用“统一工具壳 + 三个独立工具页”的结构。这个文件给后续其它 Codex session 使用，避免不同工具互相改乱。

## 本地运行

项目目录：

```text
C:\Users\Administrator\Documents\场外基金实时估值预测\worktree
```

本地开发服务默认端口：

```text
http://127.0.0.1:8000
```

启动命令：

```powershell
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m uvicorn fund_estimator.api.app:app --host 127.0.0.1 --port 8000
```

## 页面架构

统一首页是 `fund_estimator/web/index.html`，只负责工具箱外壳：

- 顶部三个标签：`场外估值`、`套利监控`、`基金对比`
- 由 `fund_estimator/web/shell.js` 控制切换
- 由 `fund_estimator/web/shell.css` 控制壳样式
- PWA manifest 是 `fund_estimator/web/manifest.webmanifest`

三个工具页通过 iframe 挂载，互相隔离：

```text
/tool/estimate   -> fund_estimator/web/estimate.html
/tool/arbitrage  -> fund_estimator/web/arbitrage.html
/tool/compare    -> fund_estimator/web/compare.html
```

对用户可见的直达地址：

```text
/          默认场外估值
/estimate  场外估值
/arbitrage 套利监控
/compare   基金对比
```

## 各 session 修改边界

每个 session 动工前必须先确认自己的 scope，并在收尾前再运行范围检查脚本。不要凭感觉改共享文件。

动工前用 `--preflight`，它会打印允许修改的文件范围，并要求工作区没有未提交改动。如果已有未提交改动，先不要开工，避免混入其它 session 的东西。

```powershell
# 统筹 session 动工前
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\check_session_scope.py orchestrator --preflight

# 场外估值 session 动工前
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\check_session_scope.py estimate --preflight

# 套利监控 session 动工前
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\check_session_scope.py arbitrage --preflight

# 基金对比 session 动工前
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\check_session_scope.py compare --preflight
```

动工后或收尾前用普通检查，它会检查当前改动是否都在本 session 允许范围内：

```powershell
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\check_session_scope.py estimate
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\check_session_scope.py arbitrage
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' scripts\check_session_scope.py compare
```

如果检查失败，该 session 必须撤回自己越界改动，不能要求统筹 session 事后猜哪些能留。

统筹/整合 session 负责：

- `fund_estimator/web/index.html`
- `fund_estimator/web/shell.js`
- `fund_estimator/web/shell.css`
- `fund_estimator/web/manifest.webmanifest`
- `fund_estimator/api/app.py` 中页面路由和跨工具 API 装配
- `Dockerfile`、`docker-compose.yml`、README、部署说明

场外估值 session 只改：

- `fund_estimator/web/estimate.html`
- `fund_estimator/web/app.js`
- `fund_estimator/web/styles.css`
- 场外估值相关 API、模型、服务、测试

套利监控 session 只改：

- `fund_estimator/web/arbitrage.html`
- `fund_estimator/web/lof_app.js`
- `fund_estimator/web/lof.css`
- `fund_estimator/web/vendor/qrcode.js`（仅飞书二维码需要时）
- LOF/ETF 相关 API、模型、服务、测试

基金对比 session 只改：

- `fund_estimator/web/compare.html`
- `fund_estimator/web/compare_app.js`
- `fund_estimator/web/compare.css`
- 对比相关 API、模型、服务、测试

如果某个 session 必须改共享文件，例如 `fund_estimator/services/cache.py` 或 `fund_estimator/models/schema.py`，要先说明原因，并确认不会破坏另外两个工具。

以下文件只有统筹 session 可以修改，其它 session 禁止修改：

```text
README.md
协作说明.md
.dockerignore
.gitignore
.gitattributes
Dockerfile
docker-compose.yml
deploy/**
fund_estimator/api/app.py
fund_estimator/web/index.html
fund_estimator/web/shell.js
fund_estimator/web/shell.css
fund_estimator/web/manifest.webmanifest
scripts/check_session_scope.py
```

## 新增工具的规则

新增第四个工具时，按这个模式加：

1. 新增独立页面，例如 `fund_estimator/web/new_tool.html`
2. 新增独立 JS/CSS，例如 `new_tool_app.js`、`new_tool.css`
3. 在 `index.html` 增加一个顶部标签和一个 iframe pane
4. 在 `shell.js` 的 `TOOLS` 增加工具名、路径、标题
5. 在 `api/app.py` 增加 `/new-tool` 和 `/tool/new-tool` 路由
6. 不把新工具逻辑塞进已有工具 JS

## 验证要求

每次修改后至少跑：

```powershell
node --check fund_estimator\web\shell.js
node --check fund_estimator\web\app.js
node --check fund_estimator\web\lof_app.js
node --check fund_estimator\web\compare_app.js
& 'C:\Users\Administrator\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe' -m pytest
```

页面验收至少访问：

```text
http://127.0.0.1:8000/
http://127.0.0.1:8000/estimate
http://127.0.0.1:8000/arbitrage
http://127.0.0.1:8000/compare
```

## 重要约束

- 不再以 Armbian `/opt/fund-estimator` 作为开发源。
- 当前源码以本地 `worktree` 和 GitHub 为准。
- Docker 部署使用 `docker-compose.yml`，数据目录挂载到 `./data`。
- PWA 只做轻量 manifest 和主屏图标，不做 service worker，不缓存实时行情 API。
